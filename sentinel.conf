# specify path to brexocoin.conf or leave blank
# default is the same as BrexocoinCore
#brexocoin_conf=/home/evan82/.brexocoin/brexocoin.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
